{
  "name": "proxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "anyproxy --port 8888 --intercept --rule rules.js --ignore-unauthorized-ssl -s"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mime-types": "^2.1.26",
    "yaml": "^1.8.3"
  }
}
